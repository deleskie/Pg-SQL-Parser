- parsing:
    - function in from (select * from generate_series())
    - values()
    - aliases for source in form of "as x (a,b,c)"
    - set operations
    - cte
    - subselects
    - "weird" functions:
        - extract()
        - overlay()
        - position()
        - position(substring in string)
        - substring()
        - trim()
- cli tidy
