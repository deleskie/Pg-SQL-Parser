- parsing:
    - subselects
    - array[]
    - in (...)
    - (a,b,c) = (d,e,f)
    - extended window definitions (frame_clause)
    - "weird" functions:
        - extract()
        - overlay()
        - position()
        - position(substring in string)
        - substring()
        - trim()
- cli tidy
