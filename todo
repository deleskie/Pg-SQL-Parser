- parsing:
    - cte
    - subselects
    - extended window definitions (frame_clause)
    - "weird" functions:
        - extract()
        - overlay()
        - position()
        - position(substring in string)
        - substring()
        - trim()
- cli tidy
